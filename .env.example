# Copy to .env and set your Google Apps Script Web app URL after deploying google-apps-script.js
VITE_GOOGLE_SHEET_WEB_APP_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Optional, production only: if dashboard GET fails (CORS), set a CORS proxy URL for reading sheet data.
# In dev, the Vite server proxies /api/sheet → your sheet URL, so no CORS proxy needed.
# VITE_GOOGLE_SHEET_READ_URL=https://your-cors-proxy.example/...
